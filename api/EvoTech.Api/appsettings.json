{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Safe to commit. The signing key is NOT here — it lives in user-secrets.
  "Jwt": {
    "Issuer": "evotech-api",
    "Audience": "evotech-frontend",
    "ExpiryHours": 12
  },

  "Cors": {
    "AllowedOrigins": [ "http://localhost:3000" ]
  }
}
